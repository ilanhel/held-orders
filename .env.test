# Test environment — local embedded PostgreSQL started by tests/setup.ts.
# Fully local: fast, isolated, and never touches the Neon production DB.
DATABASE_URL="postgresql://postgres:postgres@localhost:54329/held_test?schema=public"
DIRECT_URL="postgresql://postgres:postgres@localhost:54329/held_test?schema=public"
NODE_ENV="test"
SESSION_SECRET="test-secret-key-for-vitest"
